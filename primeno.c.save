#include<stdio.h>
int main() {
	int n, i;
	printf("Enter number: ");
	scanf("%d", &n);

	i = 2;
	while(i <= n-1) {
		if(n % i == 0) {
			printf("%d is not prime no. \n", n);
		}
		i++;
	}
	return 0;
}
